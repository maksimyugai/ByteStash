{
  "name": "bytestash",
  "description": "A React + Cloudflare Workers app that stores code snippets",
  "author": "Max Yugai",
  "version": "2.0.0",
  "contributors": [
    {
      "name": "Jordan Dalby",
      "url": "https://github.com/jordan-dalby/ByteStash"
    }
  ],
  "scripts": {
    "dev": "npm run build --workspace client && npm run dev --workspace worker",
    "build": "npm run build --workspace client",
    "test": "npm test --workspace worker",
    "check": "npm run check --workspace worker",
    "deploy": "npm run build --workspace client && npm run deploy --workspace worker"
  },
  "workspaces": [
    "client",
    "worker"
  ],
  "engines": {
    "node": ">=22"
  }
}
